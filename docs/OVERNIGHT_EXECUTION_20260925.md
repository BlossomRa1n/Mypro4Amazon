# 2026-09-25 夜间退化诊断、cross复验和最终验收执行记录

截至北京时间 **05:46**，raw退化诊断、共同早停cross复验、全量合法历史候选池、最终raw全量训练和唯一100,000人test均已完成并本地验收。固定决策为 **保留raw cross，停止追加搜索**。最终训练于 **04:11:45–05:21:17** 执行，唯一test于 **05:27:36–05:40:57** 执行；finalize和远端源码/控制树的新归档也已完成。最终分析与恢复检查随后收尾。

## 用户批准的边界

窗口固定为2026-09-25 00:04:49–08:04:49，最多3轮新优化，已运行1轮。到截止时不启动新轮，正在运行的注册轮可完成并分析；不为用完额度追加实验。操作改善和唯一机制证明分别报告。所有修改前已生成主体恢复包，覆盖未提交文件、Git、配置、环境及大资产SHA引用，并实际解压和恢复验证。

顺序固定为：退化诊断与操作优化 → cross消融 → 冻结方案 → 全量训练 → 最终100k test一次验收。所有选择规则在看该轮confirm前固定；最终test不用于再选模型、停止点或超参数。正常启动、监控与读取由Low执行；实现由Medium负责，方案、高风险审计和归因由Ultra负责。每900秒记录正常运行状态。

## 已完成的结果

| 项目 | 结果与边界 |
|---|---|
| Raw早期checkpoint与epoch3 | 固定80k confirm，三seed平均HR@5由4.159583%升至4.687083%，差+0.5275个百分点，95%配对区间[+0.442083,+0.612083]个百分点；全部注册标准通过 |
| 退化解释 | 训练拟合增强、未来排序变差；未发现记录范围内数值崩溃或探针batch依赖。用户ID记忆、共享表征漂移和训练/评估分布失配未被独立因果拆分 |
| 共同早停下去cross | zero−raw平均HR差+0.0041667个百分点，97.5%区间[−0.045000,+0.052917]个百分点，一正两负，未晋级；不宣称等价或raw显著更优 |
| 原始证据归档 | Raw共511文件/8,493,758,940字节；cross run39文件/3,996,425,341字节，source/control95文件/745,209字节，模型可读、配对身份/候选/trace、逐文件SHA和来源门禁通过 |
| 本地测试 | 完整测试164项、89.024秒通过；随后来源工具和归档watcher新增定向测试通过，分别保存日志与审计SHA |

完整分析见 `TRAINING_DIAGNOSTICS_RESULTS_20260924.md`、`TRAINING_COHORT_COVERAGE_REVIEW_20260925.md` 和 `OVERNIGHT_CROSS_RESULTS_20260925.md`。原协议、原统计和冻结模板未被回写。

## 最终阶段合同

去cross未晋级，最终模型集合只能为raw一个。固定seed42/init424242、4,731,777条合法prefix行、完整1epoch、18,484次update、末batch129；FP32、batch256、BPR mixed16、原AdamW及Cosine T_max=3不变。训练pool权重[2,0,0.7,0.05]，最终评估pool权重[2,1,0.7,0.05]，75候选，ItemCF300和180天半衰期保持原合同。

全量1epoch用于确保所有test用户实际消费过自己的合法历史训练目标。这是预先声明的规模迁移选择，不能当作fast0.25/0.5epoch规则的逐字复制，也没有full3对照来证明全量退化已被实验解决。最终100k只报告锁定模型绝对指标、覆盖及限制，不据test追加开发。

独立final executor、model准备锁、一次test守卫和本地归档验证器均已通过审计及真实小型链路测试。正式入口仍须依次通过：full pool本地来源/语义/传输 → 实测资源与时间 → 精确授权映射及plan → 完整训练与本地checkpoint可读/UID覆盖 → model准备锁 → 唯一test会话 → 全部本地验收。

## 全量准备与已解释的部署故障

全量训练候选池于北京时间03:37:45完成，覆盖4,731,777行，构建加验证计5,073.19秒（84.55分钟）。正式pool的8文件、1,476,351,420字节及15文件source/control、77,277字节均完成本地逐文件SHA核验；2,048行pilot与full、128行旧cache与full的候选顺序/长度也通过精确比较。完整来源门禁通过后，149项最终依赖映射已验证。

首次prepare在初始依赖检查阶段因远端缺少原身份封印 `SHA256_MANIFEST.json` 退出1，未加载候选、训练或test。经独立审计，仅排他新增原封印的949字节文件；已有55文件的SHA保持不变，旧失败产物全部保留。在相同授权、相同源和独立恢复目录下，prepare于03:58:48退出0，锁定最终plan；具体证据见 `FINAL_PREPARE_IDENTITY_SEAL_RECOVERY_AUDIT_20260925.md`。此故障是依赖部署缺件，不是训练效果失败；不能删除日志后写成一次无故障运行。

最终训练启动预检记录数据盘可用4,654,669,824字节、所需门槛3,762,912,620字节，GPU空闲，固定截止前尚余13,983秒，通过9,000秒门槛。唯一monitor/训练PID为14057/14058；900秒记录正常资源状态，本地独立watcher只观察完成状态并归档、校验，不启动训练或test。实际18,484步训练完成，4,731,777行、796,197名用户、75,708,432个负例，全部100,000名test用户都有合法历史被消费。训练及控制归档13文件、1,270,298,918字节，SHA、可读性、有限值和顺序/覆盖门禁通过。

准备锁绑定唯一raw checkpoint后，最终test只启动一次，monitor/child为16327/16328。原始身份目录的全局marker与session marker原字节一致，未复制锁到新位置绕过守卫。test于05:40:57退出0；test run/control共13文件、80,977,652字节完整下载并通过语义与传输验收。HR@5=0.0452、NDCG@5=0.0204711370、pool_hit=0.11871；候选GAUC=0.8021323507，仅在11,871名候选内正负皆有的用户上有效。

## 成本与当前状态

Raw九个epoch纯训练均值469.80秒；最初按4,731,777/604,511行数比例估计完整1epoch约61.29分钟，预留70分钟。最终实际纯训练62.02分钟，含前置校验的监督阶段69.52分钟；test监督阶段13.34分钟。候选、训练、测试与归档计时分别记录，不能把纯训练时长当成完整夜间成本。

效果边界：开发confirm已经参与选择；三seed配对区间先对每名用户求平均再按80k用户bootstrap，不视作240k独立观测。当前资产采用完整prefix聚合和静态catalog近似，并非逐行或全球日历严格因果。full1是预先锁定的规模迁移，不存在full3最终对照。最终raw-only的结构收益字段为null，未注册业务阈值；完整执行不表示所有因果问题或业务目标已获证明。

GitHub SSH只读检查成功，远端当前HEAD为`30c17af8255ccc40fbf933774005b303a518595a`；本次新代码文档尚未提交推送。项目大资产、用户身份及服务器证据保留在Git忽略的本地`server_snapshot/`，不自动公开上传。

finalize已实际退出0，原始FINAL_COMPLETED.json已下载；其SHA为`4cfac4fbaab15ed848fc07825592c4f5a5fee93d462ab9dc527d54978b29104b`。随后远端最终源码/控制树237文件、3,367,517字节完整归档，传输前后清单一致且逐文件本地SHA相等。Low运维writer在北京时间05:46:28结束写入，所有后台监控和归档进程均已退出。

只有必要证据可读、异常解释完整、最终报告/总结完成、汇总验收和恢复检查通过后，才允许关机。此执行记录不替代独立关机授权；后续真实收尾结果见`OVERNIGHT_CLOSEOUT_20260925.md`，本文件不再根据关机或Git结果回写。关机后须以全新SSH或等价状态确认，再提交推送项目变更并暂停续办自动化。
