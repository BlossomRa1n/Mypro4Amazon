# 午后V2验证：最终收尾

时间：北京时间2026-09-25。**本轮单因素实验、三seed开发评估、完整本地归档与独立恢复已完成；未晋级，保留原方案。服务器已执行关机并通过两次全新SSH连接拒绝核验。**

## 结果与时间

本轮唯一训练改动是候选RRF的V2权重0→1，其他合同固定。旧/新三seed平均confirm HR@5分别为4.687083%和4.603750%，差−0.083333个百分点，97.5%区间[−0.141250,−0.027083]个百分点；NDCG也下降。四项注册门槛全部失败，保留semantic_concat、raw cross、随机用户初始化、旧训练pool及screen早停。没有新增全量训练或重评旧10万人test。

工作于17:44:45开始，20:45:16满足全部安全条件后放行关机，持续至少10,831秒；关机实际开始20:46:03，超过三小时。训练之外的时间用于实现、smoke、独立审计、配对统计、归因、档案传输、闭包修复及实际冷恢复，没有为了延长时间追加未注册训练。

## 恢复与安全条件

| 项目 | 已核验结果 |
|---|---|
| 正式run | 39文件、3,996,448,502字节；源码、初始化、训练行序、负例来源、候选与身份配对、数值有限性及bootstrap重算通过 |
| 主恢复包 | 项目父目录`recovery_capture_v1/experiment_recovery.tar.gz`，64,690,743字节，SHA `157e69a28d5233a9779eec399971ce7df2d9047ac1b45eb047671c0c878d8925` |
| 补充恢复包 | 项目父目录`recovery_supplement_v1/experiment_recovery.tar.gz`，64,931,478字节，SHA `a766da9de9bde50fe0213a63c271fd731fc0beacfdff59d53eb4058dc5f9b780` |
| 最终依赖范围 | 352外部引用、15,255,010,563字节；273份补充包回执；247个源码文件；数据/模型/身份独立保留本地 |
| 独立恢复与闭包 | 实际解包、Git还原、源码SHA/mode、refs/status、补丁、全部外部引用前后核验通过；257当前依赖与93旧嵌套依赖无遗漏 |

两包及外部资产必须一起保留；压缩包不是完整独立的数据备份或环境镜像。56个必需路径和5个旧备份路径的遗漏已通过补充清单修复，旧包保持原字节。详细恢复指引见`AFTERNOON_V2_RECOVERY_GUIDE_20260925.md`。文献核验声明的错误、恢复兼容修复以及未执行的被拒绝操作都有明确记录，见关机前记录；无未解释的正式训练失败。

20:41:42最后远端预检无训练、评分、候选构建、监控或传输任务，GPU空闲，6个完成标记的SHA均与归档一致。系统盘剩3,224,002,560字节，数据盘剩3,113,222,144字节，满足1.5GiB/2GiB预留。本轮分盘无需扩容。下一轮若保留旧证据并串行保存六份同大小模型，按当前余量估算，含原子替换临时模型、数组日志及磁盘预留的系统盘新增需求约7.2GiB；10GiB仅为容量建议，最终启动仍须重新核算。

## 一次关机及独立连接核验

20:46:03.896仅执行一次`/usr/bin/shutdown -h now`。20:46:04.020原SSH断开，退出码255、输出为空，无本地超时；不把断连写成exit0，也没有重试关机。

20:46:36.624和20:47:10.299分别使用禁用旧socket、ControlMaster及ControlPersist的全新SSH，两次都返回`Connection refused`。这满足约定的关机后网络不可达标准，区别于认证失败；没有平台物理电源或账单遥测。

| 独立收尾凭据 | SHA-256 |
|---|---|
| 项目父目录`independent_supplement_cold_restore_audit_v1/INDEPENDENT_COLD_RESTORE_VERIFIED.json` | `7d4a5abb0db858c190edda82b9cd8f988927c1fb71ca29af77454b11b7760898` |
| 同目录`INDEPENDENT_COMBINED_REPLAY_CLOSURE_VERIFIED.json` | `62610bc66e241126b82751a1cf0b80719cc94e0d7ae3f6ff44e8a651a4e1eac1` |
| 证据根`shutdown_gate_v1/ROOT_SAFE_TO_SHUTDOWN.json` | `2cd16bfc37bf0fcd71253f5d10a74c2eaf396c9eb7ea74d0d20cc0de4f7ae2e2` |
| 证据根`shutdown_execution_v1/SHUTDOWN_VERIFIED.json` | `b900aa238ea4d74b03eae747a7cefe24f12784406aecceb049071687e141a5ab` |

证据根为`server_snapshot/optimization_20260925_evening/`。本文件产生于两份恢复包之后，不声称已经包含在较早归档里；它随最终Git提交保留。提交SHA、实际push输出、远端HEAD验证和自动续办暂停结果放在证据根的`git_closeout_v1/`，避免提交内容引用自身SHA。原数据、模型、身份、服务器凭据及私人恢复包不推送GitHub。

## 下一项建议

先研究用户embedding相对学习率，候选为现有1×对0.1×，固定旧pool、raw结构、负例流、训练顺序和评估方式；数值及门槛须下一轮先注册。该学习率同时影响user/cross路径及AdamW衰减，不能把效果解释为纯粹的记忆机制。暂不叠加cross或其他结构改动。

本轮fast基线已经通过共同合同校验，无须重训；若下一轮改成全量预算、不同seed或停止规则，需要匹配的控制。旧10万人test已经使用，重训不会使其重新独立；新的泛化验收需要未参与开发选择的新评价信息。
