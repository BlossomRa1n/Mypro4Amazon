# 离开期间的优化记录

## 授权与执行边界

- 用户已确认将所有压缩包复制到项目以外，并授权本项目与指定服务器的连接、读写、实验执行及必要决策。
- 本地项目：`D:\MyPro-Amazon`。服务器：`connect.westb.seetacloud.com:48427`。
- 决策采用推荐方案，记录依据、参数、验证结果和放弃原因；不反复等待用户确认。
- 所有适用阶段结束、结果同步核验成功、无运行中的训练任务后，执行服务器关机。仅完成核心批次不代表全部工作结束。
- 保留旧代码与模型；不覆写基线。新增实验独立目录。测试集不得用于方案选择。

## 备份

- `backups/Amazon_Core_Before_Optimization_20260912.zip`
- 70 个文件，258946 字节；包含核心代码、测试、工具、文档和运行脚本，不包含数据、权重、环境或 SSH 凭据。
- SHA-256：`14d7c40fe09d99c866ca787b762dbdacd8eac8b5f9789ca66bfb0ef3c139eaad`
- ZIP CRC 和逐文件 SHA-256 全部核验通过。用户已复制，后续不修改此压缩包。

## 阶段清单

| 阶段 | 状态 | 交付或门槛 |
| --- | --- | --- |
| 核心代码备份 | 完成 | ZIP 与清单已核验，用户已复制 |
| 修复后的正式基线 | 完成并已同步 | 2026-09-12 16:12:42 正常退出，结果与预测已归档 |
| 候选池与失败分组诊断 | 完成并已同步 | 扩池初筛未获益，保留 100；详见下表 |
| DIN 随机/混合难负样本对照 | 完成 | 随机续训有效，混合难负样本退化 |
| SASRec 继续训练 | 完成 | 额外 2 轮的端到端改善未通过确认 |
| 有效改动复验及组合 | 完成 | 随机 DIN 续训在种子 43 上复验通过，保留原 SASRec |
| 候选负样本与排序损失 | softmax 对照、内部候选准备完成 | softmax 对同分布 BPR 无可靠收益；400000 用户候选已准备，候选训练排队中 |
| 候选融合 | RRF 验证通过 | HR@5 3.0244%；学习式融合仍需独立生成训练样本 |
| 精排结构消融 | 完成，保留原入选模型 | 对照、移除用户 ID、品类 32 维、移除热度、Dropout 0.2 均已完成；无结构改动入选 |
| raw-slice / RankMixer | 正式 CUDA 等价验证通过 | raw-slice 无损；RankMixer 是否适用仍待证据判断 |
| 新召回通道 | 品牌通道初筛完成 | 原始并集独有覆盖 4.26%；与入选 RRF 的组合仍待做 |
| 效率、最终评估与归档 | 待执行 | 固定最终方案后评估测试集；保存结果、代码与运行命令 |
| 同步核验与关机 | 待执行 | 所有适用阶段结束且本地同步完成后才允许 |

## 已确认的基线结果

每个 split 固定 100000 用户。新协议与历史实验不同，不能直接宣称对旧结果的模型增益。

| 指标 | Validation | Test |
| --- | ---: | ---: |
| V2 HR@100 | 4.886% | 4.086% |
| ItemCF（含热门兜底）HR@100 | 9.033% | 7.663% |
| 候选池覆盖率 | 9.843% | 8.443% |
| 召回分数直接排序 HR@5 | 1.881% | 1.493% |
| 纯 DIN HR@5 | 2.430% | 2.081% |
| 纯 DIN NDCG@5 | 1.5624% | 1.3485% |
| 候选命中后的 DIN HR@5 | 24.688% | 24.648% |

数据标识：`0c204a272d76c032a87326b95963eabd7976b0fe21e146b74b063bd3e7834faa`。
服务器基线目录：`/root/corrected_baseline_20260912`。

## 决策记录

### 2026-09-12：开始核心优化

- 基线正常完成，两塔与 DIN 验证曲线都持续改善。SASRec 额外训练有检验价值。
- 候选覆盖率约 9.84%，进入候选后的排序成功率约 24.69%，召回与精排均有损失；先做候选预算和负采样对照。
- 扩大候选可能增加覆盖而降低前五命中，因此同时记录覆盖、HR@5、NDCG@5 和耗时，不仅追求候选命中。
- 先实现可恢复的核心队列，再依据结果推进后续阶段；预计时间不是效果保证。

### 2026-09-12 18:59：正式核心队列启动

- 基线结果已同步到 `server_snapshot/2026-09-12/corrected_baseline_20260912/`，压缩归档为同级 `corrected_baseline_results_20260912.tar.gz`。本次同步含结果、配置、预测、日志，不含大型权重和数据缓存；最终仍需同步入选权重。
- 本地及服务器各 12 项测试通过；两端小规模核心全流程均产生 `CORE_COMPLETED.json`。服务器验证于 18:56:17 完成。
- 正式命令：`nohup flock -n /root/optimization_20260912.lock python -u code/run_optimization.py --baseline /root/corrected_baseline_20260912 --run-dir /root/optimization_20260912 --screen-users 10000 --confirm-users 90000 --hours 3`。
- 工作目录 `/root/optimization_src_20260912`，训练 PID 9433，锁进程 PID 9432；日志 `/root/optimization_20260912.log`。18:59 已进入 100 候选、10000 用户的候选构建。
- 独占锁防止重复启动。3 小时为阶段间检查的预算，达到预算只暂停，不关机，不将未做阶段标为完成。
- 启动时系统盘约 17 GiB、数据盘约 14 GiB 可用。运行中的源代码冻结；后续阶段使用独立来源清单。
- 后续所有验证比较仍属于自适应开发过程；重复查看同一确认集会削弱单次置信区间的确认意义，最终另留未参与开发的验证用户作一次锁定方案验收，再评估测试集。

### 2026-09-12 19:15：候选池结论与后续队列

固定基线模型、固定 10000 初筛用户，纯 DIN logits 排序：

| 候选预算 | 候选覆盖率 | HR@5 | NDCG@5 | DIN 批量评估耗时 |
| --- | ---: | ---: | ---: | ---: |
| 100 | 9.89% | 2.32% | 1.5000% | 9.15 秒 |
| 200 | 13.23% | 2.15% | 1.3959% | 16.24 秒 |
| 300 | 15.15% | 2.10% | 1.3594% | 22.59 秒 |

- 结论：单纯扩池提高覆盖但降低前五命中，同时增加精排开销；当前保留 100。200/300 未入围，所以没有对它们再跑 90000 用户确认，不声称已统计证明其劣于 100。
- 额外 90000 用户的 100 候选参考为覆盖率 9.8378%、HR@5 2.4422%、NDCG@5 1.5694%。以上耗时是批量吞吐测量，不是在线单请求延迟，也不包含召回与候选构建。
- 分组发现：确认集里历史超过 20 条的用户 HR@5 为 1.2751%，训练热度 1-5 的目标 HR@5 为 0.2174%，后续优先检查历史噪声和长尾排序；这些是描述性诊断，不代表原因已证明。
- 结果、分组与逐用户 NPZ 已同步到 `server_snapshot/2026-09-12/optimization_pool_results_20260912/`。压缩包两端 SHA-256 一致：`961e70092ae6528fe60f6c6ec6c02971c348e05585be95bd0e0b403caa2a861f`。
- `code/run_followup.py` 已实现收益复验、相同负采样下 BPR/softmax 对照、组合验证。复验改变的是续训的顺序/采样种子 42→43，并非从零重训整套基线；报告中不会称作完整独立训练复现。
- 只有核心确认有收益的 DIN/V2 改动才复验；softmax 对照仍会执行。若无可靠提升，保留基线并记录失败实验。训练起点、学习率、负样本数和步数保持一致。
- 两端共 14 项单元测试通过；两端后续小规模流程正常产出 `FOLLOWUP_COMPLETED.json`。小规模零命中结果只验收程序流程，不能当作模型结论。入围分支还需要正式结果触发后验收。
- `code/optimization_models.py` 新增 raw-slice 与缓存用户编码的推理路径。本地 CPU 对拍逐元素完全一致：eval logits、训练 loss、全部梯度、连续 3 次 AdamW 更新和 BatchNorm buffer。空候选、变长候选及同分排序测试通过。正式全量 checkpoint/CUDA 对拍及吞吐测量仍待做，尚未切换生产评估路径。
- 后续来源 `/root/followup_src_20260912` 已冻结，压缩包两端 SHA-256：`892efc00c7cc9ae82280e2249c5f266373da7f36ebbde288f6a3524951d0c075`。核心来源保持不变。
- 后续队列 PID 10037 正在等待 `/root/optimization_20260912.lock`，命令：`nohup flock /root/optimization_20260912.lock python -u /root/followup_src_20260912/code/run_followup.py --baseline /root/corrected_baseline_20260912 --core-run /root/optimization_20260912 --run-dir /root/followup_20260912 --screen-users 10000 --confirm-users 90000 --hours 3`。日志 `/root/followup_20260912.log`。
- 后续脚本要求核心完成标记存在。若核心因时间/磁盘预算暂停，它会拒绝继续，监控应先恢复核心，再重新排队；不能把这一情况误报为全部完成。
- 保留确定性验证用户序列的 `[100000:200000]`，直到最终方案锁定才验收。已有 100000 开发用户仍只作开发与诊断；测试标签不得用于调整方案。

## 接续操作

1. 以 `optimization_state_20260912.json` 的 current_stage 和对应路径检查实际进程。旧 PID 为历史记录；不要重复启动队列，也不要向已运行的冻结源目录添加或修改 Python 文件。
2. 核心与后续队列运行期间，继续准备独立版本的配额/RRF、ItemCF 历史衰减、品牌等互补通道，并严格保持候选资格和预算一致。新通道要报告独有命中和端到端收益。
3. 精排结构优先单独比较删除用户 ID、品类瓶颈与正则化；原始值切片先做正式 CUDA 等价检查。是否增加小型残差 mixer 由前序诊断决定，不能预先把复杂化当作收益。
4. 真实召回候选训练/学习融合必须使用训练内部切分或交叉拟合，不能将已看过训练目标的召回分数伪装成离线生成的无泄漏特征。
5. 各队列完成后归档验证结果，推进尚未完成的适用阶段，再锁定最终方案做保留验证用户验收、测试评估、效率测量和最终同步。仅在全部验收并确认无训练时关机。

### 2026-09-12 22:43：用户请求工作汇报后实际核查

- 核心队列于 20:15:43 完成，后续复验/损失/组合队列于 21:03:41 完成，两者均有完成标记。
- 22:43:34 检查时没有相关训练进程，GPU 利用率及显存均为 0。后续阶段未自动接上，GPU 已空闲约 100 分钟。这一衔接没有做好；不能继续报告为“训练正在运行”。
- 本地自动化配置仍为 ACTIVE，但仅有配置不等于实际完成了调度。原因尚未确认，不推断是应用、网络或调度器的问题。随后本轮直接准备并部署融合实验。
- 已完成结果及逐用户评估同步到 `server_snapshot/2026-09-12/optimization_20260912/` 和 `followup_20260912/`。归档 `optimization_completed_results_20260912.tar.gz` 两端 SHA-256 一致：`90b1bb08d985a29e7ab2f650c7e0c3cdb320845158609425827bda81c2fec797`。本次不含大型权重，最终模型归档仍待做。

固定 90000 确认用户、100 候选，以下均为验证集结果：

| 配置 | HR@5 | NDCG@5 | 结论 |
| --- | ---: | ---: | --- |
| 修复基线 | 2.4422% | 1.5694% | 参考 |
| DIN 随机负样本继续训练 1 轮，种子 42 | 2.5733% | 1.6676% | 采用；HR 相对提升 5.37% |
| DIN 混合难负样本继续训练 1 轮 | 1.7356% | 1.0792% | 不采用 |
| SASRec 继续训练 2 轮，DIN 固定 | 2.4611% | 1.5786% | 小幅点估计提升，区间跨 0，不采用 |
| DIN softmax，相同随机负样本分布 | 2.5778% | 1.6702% | 相对 BPR 无可靠提升，不采用 |

- 随机 DIN 续训相对基线：HR@5 +0.1311 个百分点，成对 bootstrap 95% 区间 `[+0.0811,+0.1811]` 个百分点；NDCG@5 +0.0982 个百分点。
- 种子 43 的 DIN 随机续训：HR@5 相对基线 +0.1333 个百分点，95% 区间 `[+0.0822,+0.1844]` 个百分点；支持续训收益。这不是整套模型从零训练的独立种子实验。
- 混合负样本相对随机对照：HR@5 -0.8378 个百分点，区间 `[-0.9256,-0.7522]`。它说明当前 ItemCF 难负样本比例/方式有害，不能推广为所有难负样本都无效。
- softmax 相对同分布 BPR：HR@5 +0.0044 个百分点，区间 `[-0.0178,+0.0267]`；没有理由为这个差异换损失。
- 当前选定“原 SASRec + 随机负样本续训 DIN + 100 候选”。收益来自续训及学习率安排，不归因为新结构或难负采样。
- 新增融合实验脚本 `code/run_fusion_experiments.py`：固定上述模型，比较原配额、RRF、ItemCF 30/90 天历史半衰期、品牌补充通道。保留原始并集覆盖、各路覆盖和品牌独有命中，防止把渠道重叠误作增益。先 10000 初筛，再对入围配置做额外 90000 确认。
- 融合测试曾发现测试用 NumPy 时间数组只读；通过复制测试数组修正了测试夹具，未修改正式数据。最新本地 16 项测试通过，配额候选逐项与冻结基线一致，小规模融合流程已通过。

### 2026-09-12 23:20：恢复正式融合实验

- 服务器 16 项测试通过；服务器小规模 GPU 融合流程于 23:19:36 完成，产生 `FUSION_COMPLETED.json`。
- 冻结源 `/root/fusion_src_20260912`，源压缩包两端 SHA-256：`464f3ec14422417a6d9d83dfa4e2a6d4ffcd19d14b55edaa05c3826b38b4e450`。
- 正式进程 PID 16367，锁进程 16366；仍使用同一把 `/root/optimization_20260912.lock`，避免重复 GPU 任务。
- 命令：`nohup flock -n /root/optimization_20260912.lock python -u /root/fusion_src_20260912/code/run_fusion_experiments.py --baseline /root/corrected_baseline_20260912 --followup-run /root/followup_20260912 --run-dir /root/fusion_20260912 --screen-users 10000 --confirm-users 90000 --hours 1`。
- 日志 `/root/fusion_20260912.log`，结果 `/root/fusion_20260912`。本批结束也不代表整体完成；结构消融、真实候选训练、正式 CUDA 等价/效率验收、最终留出评估与权重归档仍待处理。

### 2026-09-13 00:18：RRF 入选、结构对照与候选训练衔接

- RRF 正式融合批次于前一日 23:29:10 完成。固定随机续训 DIN 和 100 候选，90000 确认用户 HR@5 从配额的 2.5733% 提高到 **3.0244%**，相对提高约 17.53%。
- 配对 HR@5 差值 +0.4511 个百分点，95% 区间 `[+0.3756,+0.5256]` 个百分点；NDCG@5 差值 +0.2931 个百分点。候选覆盖率为 9.4022%，说明改善来自候选构成及其与排序器的配合，不能解释为覆盖率提高。
- 10000 初筛用户 HR@5：配额 2.40%、RRF 2.94%、CF 30 天衰减 2.45%、90 天衰减 2.48%、品牌配额 2.46%。品牌通道增加原始并集独有覆盖 4.26 个百分点，有互补信号，但不等于最终 HR@5 收益；保留后续 RRF+品牌组合检查。
- 融合结果已同步到 `server_snapshot/2026-09-12/fusion_20260912/`。归档两端 SHA-256：`5e8cbc9b9bdadaa450b79ec9c6f93053fde4a27dbee0b7fcc99b8cafe7c55446`。
- 新增 `structural_models.py`、`run_structural_experiments.py`。各结构分支从相同已入选 DIN 出发，使用相同学习率、随机负采样和训练量；增加继续训练对照，区分结构收益和多训练一轮的收益。用户 ID 消融同时移除其直接特征与 user-item 乘积；品类 32 维通过 SVD 及第一层权重转换初始化；热度消融同时屏蔽重复的两个热度输入。
- 正式 accepted checkpoint 已通过 CUDA FP32 逐元素等价检查：原 DIN 与 raw-slice 的 logits、全部梯度、连续 3 次 AdamW 更新和 BatchNorm 状态完全一致。短程对拍产生的模型不用于后续训练或评估，耗时测试重新加载入选权重。
- 1024 用户、100 候选、预热后三次评估：原路径中位数 0.9714 秒，缓存用户编码路径 0.8696 秒；批量吞吐约提高 11.7%，耗时约减少 10.5%，完整排序一致。这不是在线单请求延迟，尚未把该路径替换为所有实验的默认评估器。
- 结构批次锁 PID 17612，源 `/root/structural_src_20260912`，结果放在数据盘 `/root/autodl-tmp/structural_20260912`，日志 `/root/structural_20260912.log`；最多 3 小时阶段预算。源包两端 hash `c578de8da9052157a6f6a35a99e6ccae3f91726e2fd91b71b1f5338d0811ed18`。首次对照已开始训练。
- 新增 `prepare_candidate_training.py`。从原训练行构建内部 leave-two-out 数据，ItemCF、热门和品类统计仅拟合内部训练部分，在内部验证位置挖掘真实三路召回候选；完全不使用外层 val/test 标签。暂不包含内部 SASRec，不能将其描述为完整四路候选或学习式融合训练集。
- CPU 候选准备锁 PID 18185，源 `/root/candidate_src_20260912`，输出 `/root/candidates_20260912`，日志 `/root/candidates_20260912.log`；400000 用户，`CUDA_VISIBLE_DEVICES=''` 且 `nice -n 10`，与 GPU 结构训练并行。00:17 已完成内层 CF 并挖掘约 56% 用户。源包两端 hash `428311df72fa9fe0093bd33cb7a0852f833e0d63d6d56607d923baf7428b9e7b`。
- `run_candidate_experiments.py` 已完成两端小规模流程和最新 20 项测试。正式训练从结构阶段入选权重出发，在完全相同的内部目标上比较一轮 4 随机与 3 随机+1 召回负例，并按验证证据复验种子 43。降低召回负样本比例是对前次 2 个难负例退化的有依据尝试，不保证有效。
- 候选训练等待锁 PID 18466，源 `/root/candidate_train_src_20260912`，输出 `/root/candidate_train_20260912`，日志 `/root/candidate_train_20260912.log`；同一 GPU 锁，必须先存在结构完成和候选准备完成标记。源包两端 hash `9bd549293f94414641ea15ce7938e64b90002fea4cb5bbef4fc2d0c8b8ceadb3`。
- 仍待：结构/候选实际结果、RRF 与品牌/衰减的有限组合检查、是否值得增加 mixer 的证据判断、锁定方案后的新验证用户验收、最终测试、完整模型与推理产物归档。不能将当前开发集收益当作最终测试提升。

### 2026-09-13 00:54：最终验收链路已接入

- 内部候选准备完成：400000 用户，真实目标在三路内层召回池中的覆盖率 8.8845%，每人平均 99.848775 个合格负候选，无空候选池。该覆盖仅是训练内部诊断，不与外层验证/测试指标混比。
- 结构实验仍在推进。已完成的单轮训练初筛：继续训练对照 HR@5 2.74%、训练损失 0.00141575；删除用户 ID 的 HR@5 2.94%、损失 0.0289107。两者训练耗时约 22.2/12.6 分钟。仅是初筛点估计，不能据此宣布删除 ID 无损或入选。
- 新增 `finalize_optimization.py`，依次执行：固定入选 DIN 后比较当前融合/RRF+品牌/RRF+90 天衰减 → 开发集初筛与确认 → 写入 `LOCKED_SCHEME.json` → 保留验证用户 `[100000:200000]` 进行一次二元验收 → 方案冻结后测试集 100000 用户比较 → 效率测量与可运行模型包导出。
- 保留验证验收失败只退回原始修复基线，不再利用该批用户调参；测试结果不改变已确定的方案。小规模流程中的 heldout/test 只属于独立 smoke 数据，不曾读取正式保留验证和新方案测试指标。
- 条件结构门槛有明确记录：若继续训练对照已经具有极低训练损失（<0.005）而初筛不优于原入选模型，当前没有支持增加容量的证据，暂缓额外 mixer。若这个条件不成立，脚本暂停到证据复核，不自动宣称可跳过。raw-slice 已实际实现并通过正式 CUDA 等价验收。
- 当前融合方案是经验证的 RRF；四路学习式融合仍是后续研究，必须先生成含内部 SASRec 的完整训练特征。已做的三路内部候选不能假称已经满足四路学习融合要求；本轮不声称训练过标准 HSTU/MIND 或 RankMixer。
- 新增 `optimized_inference.py`，支持模型包校验、独立命令运行以及 `latest/val/test` 模式。`latest` 通过只读视图使用全部已观察历史，包含最后一次事件，并过滤完整已见集合；不篡改基线数据时间戳。
- 模型包包含 DIN/V2 权重、同源特征数据、ItemCF、配置、源代码与逐文件 SHA-256。源码复制而非链接到可变工作区；跨盘复制采用临时文件再替换，避免中断留下伪完整产物。
- 验收会从导出的 `release/code/optimized_inference.py` 启动独立子进程，与同配置测试推荐逐用户对拍，再验证 latest 推荐。样例 CSV 与完整测试推荐 CSV 分开保存，不将 32 用户 smoke 当作完整部署规模证明。
- 最新 21 项测试在本地和服务器通过。两端完整小规模最终流程通过；服务器还实际测试了 `run_final_when_ready.py` 的等待/加锁/子进程衔接，00:48:30 正常退出。
- 最终源 `/root/final_src_20260912` 已冻结，两端源包 SHA-256 `1098ee1872fe7e3902051d80756b4f437cca7c1b3ef7d7d4f9d9816e941cdac0`。
- 最终启动器锁 PID 19554，状态 `/root/final_launcher_20260912.json`，日志 `/root/final_optimization_20260912.log`，输出 `/root/final_optimization_20260912`。它先等待候选训练完成标记，再申请 GPU 锁，避免多个等待任务抢锁导致跳过前置阶段；等待上限 5 小时，最终评估预算 2 小时。
- 此启动器不会关机。只有模型包、全部指标与必要复现产物同步本地并完成校验、报告完成、确认无训练进程后，才由代理按原授权执行关机。

### 2026-09-13 01:22：工作汇报现场核查

- 服务器时间 01:21:47，GPU 利用率 90%，显存 6423/32760 MiB；系统盘可用约 11 GiB，数据盘约 9.3 GiB。结构训练主进程 17613 正常，正在训练第五个 Dropout 0.2 分支。
- 前四个结构分支已产生完整单轮历史。10000 用户初筛 HR@5：继续训练对照 2.74%、移除用户 ID 2.94%、品类 32 维 2.67%、移除热度 2.44%。原入选方案为 2.94%，目前不能宣称结构改动带来收益；最终采用与否仍由完整流程判断。
- 候选训练锁进程 18466 仍在等待；最终启动器 19555 存活，状态为 waiting_for_candidate_completion。未重复启动任务。
- 当前 90000 开发确认用户上，修复基线 HR@5 2.4422%，DIN 续训后 2.5733%，叠加 RRF 后 3.0244%，累计相对提升约 23.84%。该数字不是新方案最终测试结果。
- 最终留出验收、测试、完整模型同步校验和关机均未完成。基线、核心及融合阶段结果已在本地归档，大型最终权重仍待下载。

### 2026-09-13 01:56：结构结束与双塔指标口径核对

- 实际读取到 STRUCTURAL_COMPLETED.json：selected=accepted，保留原随机续训 DIN + RRF。Dropout 0.2 初筛 HR@5 2.64%；结构改动均未入选。候选实验正在进行 90000 用户精排评估，最终启动器仍等待其完成标记。
- 用户询问旧双塔 10.13% 与新 4.886% 的差异。旧服务器 evaluate.py:23 使用用户内 80/20 切分，:130 将验证商品聚合为集合，:235-238 与 Top-100 有任意交集即记一次命中；新 baseline_data.py:100 每个验证用户只有倒数第二个指定目标。两者均为全库 Top-100，但目标集合、历史和用户集合不同，不能直接判定模型性能减半。
- 旧服务器 train_v2.py:238 从完整 click_df 提取 SVD 初始化交互，存在验证边泄漏路径；缓存实际是否受污染需追溯具体 Phase 2 缓存生成过程。新 SVD 仅用训练交互。不能把全部下降归因于泄漏，也未做逐修复归因实验。
- 新训练还改变了去重、前缀历史、统计特征和负采样规则，属于重建任务与基线。旧训练目标出现在完整训练历史中属于训练任务错配，不等于每条验证输入直接包含验证目标。
- 新 V2 的 10000 用户逐轮初筛 HR@100 为 3.37%、4.15%、4.88%，额外两轮为 5.05%、5.06%。额外训练确有单路点估计改善；未采用的依据是 90000 用户端到端 HR@5 增益置信区间跨零，不是认定双塔无法继续提升。
- 如需定量分解旧到新各因素贡献，应固定用户、历史和预测列表先比较单目标与多目标口径，再按同一无泄漏协议比较训练改动；旧 10.13% 不是当前协议下已验证的门槛。

### 2026-09-13 02:34：剩余任务核查

- 候选实验日志于 02:00:32 记录 candidate_complete，selected=accepted，保留原入选模型。
- 最终启动器于 02:27:11 记录子进程退出码 0；同时实际确认最终目录存在 FINAL_EVALUATION_COMPLETED.json、BUNDLE_VERIFIED.json、LOCKED_SCHEME.json、fresh_acceptance.json、final_test.json、final_efficiency.json、scope_decisions.json 和 refinement.json。
- 本次只核查完成标记和状态，尚未复核最终指标内容，不提前宣称留出验收通过或最终测试提升。最终状态明确 local_sync_still_required=true。
- 剩余依次为：复核最终指标及范围决策；同步完整产物；本地逐文件校验及独立推理；完成报告并解释新旧双塔口径与归因限制；确认无剩余训练后关机并暂停自动监控。
