# Cross 多 seed 实验执行记录（2026-09-24）

本记录接续 `NEXT_CROSS_PROTOCOL_20260922.md` 与生效修订 `NEXT_CROSS_REVISION_20260924.md`。正式九组开发训练已于北京时间 2026-09-24 02:36:37 启动；下列工程测试和资源检查不能当作模型收益。进度记录采用实际检查时间，不代表后续自动更新。

## 生效口径

- 快速训练 100,000 用户；screen 20,000、confirm 80,000；两者互斥且并集为快速训练用户。
- 最终 test 是审计后所有剩余合格用户，不再请求固定 100,000，也不沿用全历史审计下的 20,247 警戒。
- 根据用户最新指示，只排除北京时间 `[2026-09-01 00:00:00, 2026-09-24 01:54:44)` 的评估或选型曝光。历史前缀训练允许；最终人群只能称“本月未参与评估”，不能称从未被评估。
- raw / normalized_gated / zero_cross，各运行 seed 42、43、44；其余训练、候选、比较阈值和一次性 test 要求保持预注册值。

## 已完成的工程验证

- v3 runner SHA-256：`ec8df470eab5bc3bb39978371c9d086b9a1e834b2be63dc72648616ffa9d1e22`。
- cache SHA-256：`e328ad066104df75b496450cf169e6ea47846c7c8d8194e38959b7652c914505`。
- 本地：91 项测试通过，九组合成 CPU smoke 均完成 3 epoch；不接触真实 test，不允许进入正式 final。
- 服务器：上传独立 `src_v3`；94 个源码文件逐一核对 SHA，91 项测试通过，测试耗时 9.275 秒。旧 `src` 和旧证据保留。
- v3 source archive SHA-256：`5230094f4dfdf556966c0181690719d493687b601a065b03358b765123105133`。

## 资源与存储

服务器实际报告 GPU 为 NVIDIA GeForce RTX 4080 SUPER，显存 32,760 MiB；CPU 限额 16 核，内存限额 66,571,993,088 字节。GPU 型号和容量按服务器观测原样记录。

历史用户上的 v2 两 batch FP32 预检通过，候选 ID/顺序与旧实现一致，targets 调用为零。v3 的候选及模型算法未改；范围认证仍须单独通过。该短 probe 不构成稳定吞吐或全程内存保证。

已执行不丢失内容的存储整理：六处完全相同文件按 SHA 核对后改用同盘硬链接；两份旧 structural checkpoint 从数据盘复制到系统盘，经 fsync、SHA 核验后，原路径切换为可读 symlink。没有删除实验内容。迁移和去重收据保存在本地 `server_snapshot/next_cross_20260924/`。

2026-09-24 本次核查时空闲为：数据盘 14,323,101,696 字节，系统盘约 8,641,896,448 字节。布局计划为开发 checkpoint 在数据盘、protocol/cache 和最终 checkpoint 在系统盘。系统盘原布局不能覆盖最终峰值，因此计划在开发完成、峰值已过去后，将旧 corrected DIN checkpoint（1,260,104,760 字节）校验迁至数据盘，保持原路径可读。是否执行由实际 R/N、元数据大小和当时两盘空闲决定。

不能通过移除九组开发 checkpoint 解决容量：最终流程仍会重读其全部证据。阶段容量草稿保存在 `server_snapshot/next_cross_20260924/capacity_plan/`；其早期命令中的 `src` 应使用已实际验证的 `src_v3`。

## 当前关卡与运行边界

月度来源审计、Ultra 复审和全流程资源核验通过后才能正式 prepare/dev。正式运行由 Low 启动，每 900 秒检查一次；异常交由更高推理档处理。无入围方案则保留 raw，不读取最终 test。最终结果、本地归档核验、服务器关机确认及 Git 推送情况将在本记录继续追加。

原始 raw ID、原始数据、checkpoint 和恢复包仅存放在被 Git 忽略的 `server_snapshot/`；公开报告不包含凭据或用户名单。

## 月度身份审计完成

Ultra 独立复验 296 个来源，其中 266 个恢复身份来源、30 个保守覆盖来源。各来源人数和 raw-ID hash、合并并集、历史文件、registry、closure 一致。月度排除并集为 796,912 个 raw ID；该并集跨数据版本，不能直接从当前总人数相减。当前 base 796,197 名合格用户中，778,901 名属于排除并集，剩余 **17,296 名**满足最终 test 资格。

部分来源采用包含实际评估用户的保守全集，排除人数不等于实际看过指标的人数。九月 Stage A/B 没有保存随机抽评估名单，因此保守排除其原验证总体。9 月 20 日已删除 smoke 的命令和原 manifest 仍缺失，但同次最早文档、原 runner 的固定取样方式、当时有限可达数据基座的完整构建记录支持保守覆盖，未声称精确恢复 600 人。旧本地 30 用户 smoke 的身份和 188 条交互、142 条训练样本、28 条评估记录均已重算吻合。六月及更早实验不再形成单独阻断。

- history SHA-256：`c11c428479f455f447a24cf2c581e32c2d47a9e7aa2b70f6aa4bbdad76aaf5b5`。
- closure SHA-256：`7dedeedd636512fb16355dbb678d65e149e11c414350ee92894c88f8a4153ee0`。
- 17,296 人排序 raw-ID 集合 SHA-256：`878750379c1c23082ddbcb1c7d74147318752cfc96735921e596d69e4052d6bc`。
- 36 文件可携带月度证据包：248,166,400 字节，SHA-256 `fd7f01891d6d20a611b9b62e244a2ac45af4e80acbec04c83e9b808dbdb609a5`。

审计只访问身份、训练位置和未来窗口边界；未调用 targets、生成最终候选或评估模型。正式 prepare 将再次按完整 base 验证这些人数及来源。

v3 对象生命周期补测保留 CPU 模板和 SVD，同时模拟两份 CPU checkpoint 张量存活；峰值 RSS 7.461 GiB、CUDA allocated 6.643 GiB、reserved 9.441 GiB。没有写 checkpoint、完整 epoch 或排名指标；不能视为正式全流程峰值。Ultra 采用额外余量后的 32 GiB 工作集预算，并要求启动时可用 RAM 至少 32 GiB、GPU 至少 16 GiB且无竞争训练。

## 正式 prepare 与开发启动

服务器 prepare 于 2026-09-24 完成，用时 43.734 秒。实际锁定 100,000/20,000/80,000/17,296 用户，快速训练行数 **604,511**，全量训练行数 **4,731,777**；四项 cohort 交集均为零，test 未来标签未读。训练 positions 及用户 records hash 与 Ultra 独立重算一致。

协议 canonical hash：`dcf65b10c266ba02b00d0493815a14b8af9aa4f47551d7c562a2010104409667`。初始 protocol 七个文件（107,324,555 字节）已下载本地。

所有历史包及实际 protocol 已落盘后再次核算容量。最终采用开发后迁移旧 DIN **及独立历史传输 tar** 的平衡方案；解压的来源证明树保持原位。扣除每盘 1 GiB 安全余量及 192 MiB 日志、NPZ、序列化附加预算后，开发峰值数据盘净余 441,131,984 字节，开发后迁移数据盘净余 193,544,400 字节，最终峰值系统盘净余 248,347,424 字节。Ultra 签审通过；迁移和 final 前仍须按实际增长复算。不能在服务器另外复制一整套模型做大归档包。

**正式九组于北京时间 2026-09-24 02:36:37 启动。** Low 核验运行参数、冻结源码和协议 hash 均一致；runner PID 6435、后台监控 PID 6434。开发目录 `/root/autodl-tmp/next_cross_20260924_dev` 仅由 runner 写入，启动和监控目录为 `/root/next_cross_20260924/monitor_v3`。后台每 900 秒记录进程、内存、显存、磁盘与候选/epoch 文件进度；监控不依赖当前对话持续。

首条监控无 OOM 或竞争 GPU 进程。启动脚本、锁、记录和首条快照共五个文件已下载并逐一核验 SHA。当前处于开发运行中，**尚无本轮模型效果结果**。九组固定 epoch-3 模型全部保存后才读 confirm；最终 test 继续封存。

## 增量备份

新增 `tools/archive_cross_v3.py`，仅以只读 SSH 复制已完成组的十个训练证据文件；组级 COMPLETED 早于 confirm，因此不提前抓取 confirm。下载先进入独立 partial 目录，逐项验证大小、SHA、JSON/NPZ 可读性和受限 checkpoint 加载/张量有限性，再原子发布。已有相同文件复验后跳过，漂移硬失败，失败保留 partial。工具不删除远端文件、不改训练参数、不执行关机。

7 项归档测试及完整 98 项测试通过；既有冻结 v3 合成 checkpoint 的真实读取也通过。Ultra 完成只读、路径、锁、原子发布与后置 confirm 隔离审查。该工具位于 `tools/`，不改变正在运行的冻结 `code/` 哈希。

增量目录属于训练备份，含本地归档收据且尚缺 confirm；不能冒充全局开发证据树。全局 COMPLETED 后仍须另行收齐 confirm、dev manifest、所有候选缓存和逐文件清单，并保留原目录布局供完整复验。

本地归档 worker 已于北京时间 02:50:25 启动，PID 39288；防休眠进程随 worker 存活。首轮成功，尚无完成组。任何归档异常都会留存收据并停止该 worker，不自动清锁或重新下载失败组。

02:51:37 首次 900 秒监控正常：screen、confirm 候选已封存，训练候选继续构建；runner CPU 100%、RSS 约 3.31 GiB，GPU 使用 1,930 MiB，OOM 为零。没有模型完成或效果指标。自动跟进已恢复为本任务，每 15 分钟由 Low 读取正常状态，完成或异常时推进后续审计与收尾。

03:43:41 Low 实时核查正常：seed42/raw 已完成 3 epoch 并在本地完成 checkpoint 与全部训练证据读取/哈希校验，进度 **1/9**；seed42/normalized_gated 已完成第 1 epoch 的 screen。train/screen/confirm 候选均已封存。首组每 epoch 2,362 steps，含 screen 的耗时分别为 511.68、520.19、527.48 秒，合计约 26 分钟；这只是首组观测，不包含候选预处理、最终 confirm 或归档耗时，不承诺后续各阶段相同吞吐。

该次监控 RSS 约 4.30 GiB、显存使用 10,450 MiB，cgroup OOM/OOM-kill 均为零。系统盘余 7,760,429,056 字节、数据盘余 13,061,304,320 字节。后台训练及本地增量归档均正常；尚未进入全局 confirm 比较，也未使用最终 test。

03:53:58 Low 核验定时快照和实时进程：seed42/raw、seed42/normalized_gated 三 epoch 均完成，进度 **2/9**。本地首组已发布，下一归档轮次为 03:55:11。runner、独立监控和本地归档 worker 正常，OOM 为零，尚无全局完成标记；不在九组完成前比较指标。

## 完整开发归档工具就绪

`tools/archive_cross_v3_complete.py` 已由 Medium 实现、Ultra 审核，通过 9 项自包含临时 fixture 测试及完整 107 项测试；不依赖 Git 忽略的二进制证据。工具 SHA-256 为 `4066e0206b24f69ca5e1783232876179af96e51e8b5fcb5f88b33c98d7875ca0`，没有改动正在运行的增量归档工具、冻结 runner 或 cache。

仅在全局九组及 confirm 完成后运行：远端原绝对路径上的冻结 `_validate_dev_evidence` 先复验完整证据，再复制原始布局的开发树与 protocol/cache 到两个独立本地树。已验证的本地 checkpoint 可用硬链接复用；硬链接是同一份文件的多个入口，不声称为独立灾备副本。所有收据位于归档树之外。协议目录在读取任何内容前按文件名白名单检查，每个文件哈希前再次检查，拒绝新增 final 标记或缓存。

本地完整核验包括 SHA、JSON/NPZ/NPY 可读性、checkpoint 张量有限性、共同初始化、配对用户及候选身份。保存服务器原路径，不修改封存 manifest；本地可携带校验不冒充依赖服务器绝对路径的完整历史链校验。异常保留 partial 和失败收据，不覆盖、清锁或盲目重试。工具已就绪，但截至本条尚未对真实全局开发树执行。


## 条件迁移工具就绪

两项已批准旧资产的迁移工具 `tools/migrate_cross_v3_assets.py` 已完成并通过 Ultra 独立复审。源码 SHA-256 为 `bcaca40a81090968868c9b18b8a901f617e8397409316b7f838958a8d2d50e8c`，12 项专项测试及稳定组合 119 项通过。开发中的 final 归档模块一度有三项 fixture 失败，失败证据保留；不将该次全量测试表述为通过。迁移工具没有上传或执行。

迁移只在九组完整归档并复验、冻结 compare/lock 有达标方案后使用。它要求原始完整归档回执及独立 SHA，核对官方开发证据与锁定方案；按实际两盘空闲量检查，复制后 fsync、SHA、可读性通过才原子发布目标并切换原路径为精确 symlink。永久执行锁及失败现场保留，不自动重试。成功目标与保留的 migration-partial 是同 inode 两个名称，不增加第二份文件内容。资源检查还需另核 RAM、GPU 与竞争进程。详见修订文档 §10.11。

04:09:47 Low 实时核查：仍为 2/9 完成，seed42/zero_cross 已有第一 epoch；训练、外部监控及增量备份正常。本地两组已校验，cgroup OOM 为零，系统盘余 7,760,855,040 字节、数据盘余 11,799,891,968 字节。后台定时监控按每 900 秒继续，未提前比较模型指标。


## 可重放底座与收尾工具完整验证

04:16:47 完整底座五文件发布到本地 `server_snapshot/next_cross_20260924/base_archive/`，合计 **2,038,507,950 字节**。复用了此前恢复目录中 exact SHA 的 data.pkl，另外下载 V2 checkpoint、SVD、ItemCF 和组装 manifest；服务器前后 inventory 相等，每项复制及发布后大小/SHA 均一致。底座原件不修改，服务器无写入。

V2 checkpoint 通过 CPU restricted load，93 个 tensor 有限；SVD 为 378,913×256 float32，逐块有限性通过；JSON 可解析；两份 pickle 只做全流语法遍历并核对结束，不声称语义反序列化。组装 manifest 四来源的 canonical 路径、大小、SHA 与协议对齐。原件不含 data_id/encoder hash，因此本次备份不声称重新计算这两项；其依据仍为冻结 protocol 和先前正式底座审计。完整收据位于 `base_archive_plan/`，不混入底座树。

最终阶段归档工具 `tools/archive_cross_v3_final.py` 已通过 Ultra 独立审计，源码 SHA-256 `1b86f389300c00db94aee631d45a7ce9e271daf8f58cc96142c292bce49b6ad9`。它只在全量训练封存、一次 test marker complete 后运行，验证最终四份 checkpoint、训练及测试候选、逐用户结果、锁定方案及封存证据。四份 checkpoint 的训练张量须成对一致，但顶层 final 包装元数据不同，分别保留。工具不会重新评分或启动训练；失败保留 partial 与锁。

该归档工具验证既存置信区间的形状、有限性和接受规则，不独立重算 bootstrap。真实完整数组归档后仍需由 Ultra 按冻结算法复算开发 97.5% / 最终 95% CI，并与原输出比对。

04:20 全仓测试 **127 项 / 6.202 秒全部通过**，包括三个归档工具、迁移、协议及候选缓存。此前并行开发阶段 final fixture 的三项失败已修复，旧失败记录保留。证据为 `full_repository_ready_20260924.json` 及日志，日志 SHA-256 `470530c536e359c3b8b1192d4c3486c35ee5b8ee31849fedbfa1e637a46d0d48`。冻结 runner/cache 及运行中的增量工具 SHA 均未改变。以上只是工程验证，不是新的模型效果结果。

04:22:22 Low 核查：**3/9 组训练完成**，seed42 三个变体全部三 epoch。confirm 仍为零，按协议等待九组；未产生全局完成标记。OOM 为零、三类候选缓存完整，runner/外部监控正常。系统盘余 7,760,846,848 字节、数据盘余 10,538,688,512 字节。本地增量归档已完成前两组，第三组将在下一轮 04:28:54 被收集；本条不提前声明第三组已备份。最终 test 未用。

04:41:55 Low 核查：仍为 **3/9 组训练完成**，seed42 三变体已完成三 epoch，seed43 已开始；confirm 仍为零、全局完成标记不存在。runner/monitor/cache 正常，OOM 为零；系统盘余 7,760,838,656 字节、数据盘余 10,538,434,560 字节。增量归档已发布 3/9 组，第 7 轮成功，下一轮 04:48:13；未读取指标、未启动完整归档或新实验。

04:51:37 Low 核查：**4/9 组训练完成**，seed42 三变体及 seed43/raw 均三 epoch；screen 文件 12 个，confirm 仍为零，全局完成标记不存在。runner/monitor 正常，OOM 为零；系统盘余 7,760,830,464 字节、数据盘余 9,277,181,952 字节。增量归档 worker 无错误但最新已发布仍为 3/9，下一轮按 900 秒周期继续；未读取指标、未启动新实验。

04:52:25 本地增量归档第 8 轮完成，四组已发布并通过 checkpoint/证据可读性及 SHA 校验；归档进度 **4/9**，无错误，下一轮 05:07:25。此记录仅更新本地备份完成状态，不额外读取模型指标。


## 最终阶段独立监控就绪

新增 `tools/monitor_cross_v3_final.py`，源码 SHA-256 `adddce35575b17bb81041e0e261fdbe72cdbcdf6c0440b349995427d9216ca94`，已通过 Medium 实现测试及 Ultra 独立审计。专项 11 项通过，含真实冻结 runner 的小型 CPU 产物与真实 CLI 参数契约；全仓 **138 项 / 6.595 秒通过**。证据保存在 `final_monitor_validation/`，不修改冻结训练代码。

工具默认只检查，显式执行仅启动 final-train 或 final-test 中的一个；路径、参数、900 秒监控周期固定，不能自动串联阶段、重跑、清理或关机。永久独立监控目录保存锁、原字节日志、快照及退出状态，位于封存实验树之外。启动前核 accepted 计划及原始开发证据，结束后重新核源码、计划、协议及对应完成标记；已有测试输出或缓存即使标记缺失也拒绝重测。快照只读文件进度与资源，不显示未完成指标。正常等待循环至多约 5 秒检查一次退出，但资源采集期间可能延迟，不宣称任何时刻 5 秒内响应。

该工具的阶段 complete 不替代完整归档、统计结果审查或 root 的实际 RAM/GPU/磁盘及迁移前置核验。监控异常时子进程可能仍活，必须检查现场，不能启动替代任务。各阶段结束后需将 stdout.log 原字节校验导出为 final 归档工具预期的根目录日志，并另行保存完整监控目录。截至此条工具未部署、未启动任何最终阶段。

05:08:17 Low 核查：训练仍为 **4/9** 完成，epoch screen 文件由 12 增至 14，confirm 为零。runner、外部监控、候选缓存正常，OOM 为零；系统盘余 7,760,818,176 字节、数据盘余 9,276,825,600 字节。本地增量备份第 9 轮于 05:07:39 完成，已发布并核验 4/9，与已完成训练一致。下一服务器快照为 05:21:37，下一归档轮为 05:22:39。未读取排名指标或 test，未启动新实验。

05:28:53 Low 核查：**5/9 组训练及增量归档完成**（seed42 三组及 seed43/raw、normalized_gated）。第六组 seed43/zero_cross 已有第 1 epoch；confirm 为零、无全局完成标记。候选完整，runner/monitor/归档 worker 正常，OOM 为零；系统盘余 7,760,793,600 字节、数据盘余 8,015,630,336 字节。归档第 10 轮于 05:26:27 发布并校验五组；下一服务器快照 05:36:37，下一备份轮 05:41:27。未读取排名指标、未启动新阶段，最终 test 未用。

05:47:19 Low 核查：**6/9 组训练及增量归档完成**，seed42、43 的三个变体均三 epoch；confirm 为零、无全局完成标记。runner/monitor/归档 worker 正常，无 OOM/退出/错误；系统盘余 7,760,781,312 字节、数据盘余 6,754,361,344 字节，三类候选缓存完整。归档第 11 轮于 05:45:37 校验发布六组；下一服务器快照 05:51:37，下一备份轮 06:00:37。未读取排名指标或 targets，未启动新阶段。

06:08:54 Low 核查：**7/9 组训练完成**，新增 seed44/raw，三 epoch screen 文件共 21 个；confirm 为零、无全局完成标记。补齐 05:51:37 与 06:06:37 快照并通过 SHA 校验。runner/monitor/归档 worker 正常，无 OOM/退出/错误；系统盘余 7,756,124,160 字节、数据盘余 5,492,920,320 字节，三类候选缓存完整。已发布 6/9 组本地增量归档；下一备份轮 06:15:57 收集新增完成组，下一服务器快照 06:21:37。未读取排名指标或 targets，未启动新阶段。

06:27:09 Low 核查：训练及本地增量归档均 **7/9 组完成**，epoch screen 文件增至 23，confirm 为零、无全局完成标记。22:21:37 UTC 快照已 SHA 校验；runner/monitor/归档 worker 均正常，无 OOM/退出/错误，三类候选缓存完整。系统盘余 7,756,107,776 字节、数据盘余 5,492,563,968 字节。第 13 轮归档已校验七组；下一备份轮 06:35:05，下一服务器快照 06:36:37。未读取排名指标或 targets，未启动新阶段。

06:45:12 Low 核查：**8/9 组训练及本地增量归档完成**，仅 seed44/zero_cross 尚未完成；epoch screen 文件 25 个，confirm 为零、无全局完成标记。runner/monitor/归档 worker 均正常，无 OOM/退出/错误，三类候选缓存完整。系统盘余 7,758,991,360 字节、数据盘余 4,231,344,128 字节。第 14 轮归档已校验八组；下一服务器快照 06:51:37、下一归档轮 06:54:21。未读取排名指标或 targets，未启动后续阶段。

07:19:11 Low 核查：九组训练及 27 个 epoch screen 文件全部完成，confirm 已完成 8/9；尚无全局完成标记或退出码。07:06:37 快照已下载并核对 SHA，无 OOM 或运行错误。第 16 轮增量归档于 07:14:05 完成，九组 checkpoint 和训练证据全部本地核验；worker 及其附属防休眠进程正常退出，永久锁保留。另于 07:19:01 启动任务全程独立防休眠，覆盖 confirm、条件最终阶段、完整归档及关机验证，收据位于 task_lifetime_monitor/。未读取部分 confirm 指标；下一服务器快照 07:21:37。

07:21:37 正式开发运行结束：exit_code=0、status=complete，全局 COMPLETED 确认九组完成且 test_accessed=false，九份 confirm 均存在；runner 与独立 monitor 均已退出，无错误。Low 已下载末次快照及退出收据并校验 SHA，随后一次性启动已审计完整 dev/protocol 归档。此时仍未执行 compare/lock 或最终阶段，完整校验结果待归档工具确认。

完整开发双树归档已通过，回执 run_id=20260923T232440.288270Z-4599a66e；服务器前后两次官方校验及 inventory 一致，九个 checkpoint 复用已验证本地硬链接，完整 dev 与 protocol 分别发布。07:33:49 root 又对已发布双树执行受审 verify_bundle，54.99 秒通过；包括全部 SHA、有限 tensor、候选身份、共同初始化与 screen/confirm 用户配对。原始 COMPLETE.json SHA-256 为 1a12e5999e89b78e74ac385d0f060cb5047e438cad46aaa80d098ba4a6ecd17b，独立回执 ROOT_REVERIFY.json 位于归档树之外。冻结 compare/lock 已由 Low 按一次性排他输出启动，尚未进入 final。

07:35:52 compare 与 lock 均正常结束（exit 0）：冻结选择为 no_winner，winner=null，final_evaluation_allowed=false，计划 models=[raw]。两种挑战方案都未达到开发接受门槛；按预注册分支，跳过资产迁移、全量训练和最终 test，保留 17,296 名本月未参与评估用户。07:36:35 七份 selection/plan/命令回执/日志已逐文件 SHA 下载到 control_complete，零字节日志与远端一致，退出码与输出在独立回执中可核验。Ultra 继续从完整逐用户数组独立复算后写最终分析；在完整外部日志归档、复算和报告完成前不关机。

08:01:25 北京时间前置关机检查通过。执行 `/usr/bin/shutdown -h now` 后原 SSH 返回 255；等待后使用不带 ControlPath 的全新 SSH，端口 11740 返回 `Connection refused`（退出码 255），已确认实例不可达。证据位于 `server_snapshot/next_cross_20260924/shutdown/SHUTDOWN_CONFIRMED.json`。
