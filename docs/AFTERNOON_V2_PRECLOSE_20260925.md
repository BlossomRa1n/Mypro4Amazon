# 午后单因素验证：关机前结果与恢复范围

日期：2026-09-25。本记录在正式科学结果之后补充操作事项。它不是关机成功证明；最终关机、提交和推送记录见随后新增的`AFTERNOON_V2_CLOSEOUT_20260925.md`。

## 已完成的实验

训练候选池加入V2的唯一干预已完成：100k训练、20k screen、80k confirm，三seed各完整1epoch，保持raw cross及其他训练合同。平均confirm HR@5从4.687083%降至4.603750%，差−0.083333个百分点；97.5%配对区间完全在零以下，NDCG同步下降。四项晋级条件全失败，保留旧pool。详细数字、归因限制和负例流变化见`AFTERNOON_V2_RESULTS_20260925.md`。

正式进程40.94分钟完成，退出0；没有NaN、配置漂移、丢失文件或未解释的训练失败。新run39文件约4GB、控制日志及缓存都已下载，远端inventory与本地SHA一致。独立验收重算初始化/训练行序/覆盖/配对身份和10,000次用户bootstrap。43项兼容测试通过；后续曝光分组验证与恢复修复测试另有实际回执。

实际资源采用分盘而非扩容：训练候选缓存在数据盘，checkpoint和开发数组在系统盘。20:01预检系统盘余3,225,059,328字节，数据盘余3,113,271,296字节，无本任务活动进程，GPU空闲；最晚关机前仍须新取资源与空闲快照。这轮可用不代表下一轮多模型或全量训练也够用。

## 恢复审计发现与修复

1. **主体源码恢复已验证。** 主包在项目父目录`recovery_capture_v1/`，64,690,743字节；245个源码文件、Git状态/refs和补丁均通过实际独立冷恢复。290项约14.89GB外部资产保留本地，并非全部压入65MB包。
2. **完整重放闭包随后发现遗漏。** 主包清单缺56个精确路径的必要验收/身份锁依赖，共279,122,388字节；旧备份的嵌套引用另有5个路径缺口，共23,871,248字节，其中存在同内容不同路径。全部文件本地存在且SHA匹配。主包不改写；新的补充清单必须逐项绑定这些路径，并保留角色及路径映射。只有实际补充打包/恢复/闭包验收通过后才可据此放行关机。
3. **恢复工具兼容缺陷已局部修正。** 原helper把`GIT_LITERAL_PATHSPECS`传给不接受该选项的`check-ignore`，误拒实际已忽略的项目内输出。仅此调用改为字面NUL输入并检查退出码；其他安全策略不变。10项恢复测试通过，最终以Ultra审计及补充包内真实恢复为准。旧主包内helper保留原字节。

复现入口及可复制的本地验收命令在`AFTERNOON_V2_RECOVERY_GUIDE_20260925.md`。只有源码包不能恢复全部模型、数据或操作系统环境；身份资料作为外部私人资产保存，不上传GitHub。

## 如实保留的记录错误

此前文献回执及部分后续更正声称四篇页面已经真实读取，但缺乏可观察的返回支持。所有相应声明撤回；目前只核对到本地保存的arXiv页面。原错误回执和更正索引全部保留为事故材料，不作为科学、恢复或关机门禁。模型结果以封存数组和实际独立复算为依据。

本地恢复准备期间曾重复调用已完成的准备脚本，被已有输出断言阻止；包含删除目录的操作请求被自动审批拒绝，未执行删除。误拒仓库内输出的具体原因已定位为上面的Git兼容缺陷，不能写成目录实际没有被忽略。主体成功打包只发生在独立新目录。操作更正保留在`server_snapshot/optimization_20260925_evening/closeout_supplement_preparation_v1/OPERATIONS_CORRECTION.json`，与正式科学产物分开记录。

## 关闭服务器前的最后条件

工作从北京时间17:44:45开始，必须持续至20:44:45后才允许关机。剩余期间用于完整归档、恢复、依赖核验和文档整理，不追加未注册实验。

- 新补充包实际生成并独立恢复通过；56项必需及5项旧引用缺口已覆盖，所有必要外部资产可读且SHA匹配。
- 正式科学代码/协议与封存运行一致；无运行中训练、评分、缓存、传输任务；系统/数据盘仍保留既定余量。
- 所有失败均有明确解释并保留原证据；文献记录撤回没有被错误当作已验证来源。
- 达到最低三小时后，只执行一次关机；从禁用旧socket的全新连接核验网络不可达。认证失败不算关机成功，且SSH不可达不等于直接取得平台电源或账单遥测。
- 随后提交推送明确的源码、测试、文档；读取远端HEAD确认推送，并暂停本轮自动续办。后写的关机/Git记录通过最终Git提交及独立本地收尾证据保存，不冒称它们已包含在较早的主包里。

本轮技术和统计验证完成，但优化没有成功。下一方向推荐仅研究用户embedding相对学习率；它是待注册建议，不在本轮继续跑。是否重训baseline取决于共同训练合同能否匹配：本轮fast控制可复用，改全量预算/停止规则/seed时需新的匹配控制。重训不会恢复已使用10万人test的独立性。
